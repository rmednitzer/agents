# tests/

Test suite. Mirrors the source layout: `tests/workloads/`, `tests/skills/`, `tests/harness/`, `tests/memory/`.

Tests are not optional for harness and memory; advisory for workloads and skills.
