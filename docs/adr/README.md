# Architecture Decision Records

Each ADR records one cross-cutting decision: status, context, decision,
consequences, and revisit triggers. ADRs are immutable once Accepted;
a later ADR supersedes an earlier one rather than editing it.

| ADR | Title | Status | Scope |
| --- | --- | --- | --- |
| [0001](./0001-runtime-selection.md) | Runtime selection | Accepted | Why a `Runtime` Protocol with a PydanticAI default, not a vendor SDK. |
| [0002](./0002-behavioral-contracts.md) | Behavioral contracts | Accepted | The P/I/G/R contract model, hard/soft severity, events. |
| [0003](./0003-budgets-mcp-guards.md) | Budgets, MCP, guards | Accepted | Action budgets, MCP server lifecycle, tool-use guard gate. |
| [0004](./0004-memory-namespace-contracts.md) | Memory namespace contracts | Accepted | Namespace-bound `MemoryStore`, isolation, retention, extension Protocols. |
| [0005](./0005-workload-bundles.md) | Workload bundles | Accepted | `manifest.yaml`, the loader, in-tree and out-of-tree workloads. |
| [0006](./0006-skills-and-dispatcher.md) | Skills and dispatcher | Accepted | Agent Skills compliance, registry, the dispatcher Protocol. |
| [0007](./0007-l2-implementation-wave.md) | L2 implementation wave | Accepted | Additive-to-L1 rule, lazy backends, the L2 batch delivery. |
| [0008](./0008-l3-security-hardening-and-roadmap.md) | L3 entry, security hardening, validated roadmap | Accepted | Skill-install and event hardening, the tiered L3 roadmap, CI hardening. |
| [0009](./0009-code-audit-hardening.md) | Code audit, additive hardening, errata | Accepted | The full-audit fixes, the tracked gaps, ADR 0005/0006 errata. |
| [0010](./0010-l3-default-path-wiring-and-audit-wave.md) | L3 default-path wiring, audit follow-ups, governance maturity | Accepted | `BL-100`-`104` wiring, `BL-154/156/157/161` follow-ups, cost/retry/structured-reject, REUSE/release, the dispatcher-count erratum. |
| [0011](./0011-third-audit-and-l3-capability-wave.md) | Third code audit, L3 capability wave | Accepted | `BL-172`-`180` (third-audit fixes); `BL-111` key providers, `BL-122` ABAC + audited denial, `BL-124` MVCC tokens, `BL-130` evaluation gate, `BL-131` semantic memory. |
| [0012](./0012-run-provenance-and-anthropic-capabilities.md) | Run provenance records, optional provider batch capabilities | Accepted | `BL-185` run-provenance records + offline gate, `BL-186`/`187` Anthropic/OpenAI batch + prompt-cache helpers. |
| [0013](./0013-fifth-code-audit.md) | Fifth code audit, additive hardening | Accepted | `BL-188` read-vs-listing expiry boundary, `BL-189` OpenAI batch error label, `BL-190` LocalSkillSource symlink-safe clear, `BL-191` JSON span-list memory ceiling, `BL-192` provenance-gate registry validation. |
| [0014](./0014-versioned-and-transactional-on-durable-adapters.md) | `VersionedMemoryStore` on durable adapters, `TransactionalMemoryStore` | Accepted | `BL-180`: Versioned on `RedisStore` + `DynamoDBStore`; new `TransactionalMemoryStore` Protocol + reference impls on InMemory/SQLite/Redis/DynamoDB. |
| [0015](./0015-sixth-code-audit.md) | Sixth code audit, additive hardening | Accepted | `BL-197`-`208`: Namespace TTL validation + `resolve_ttl` consolidation, RedisStore.mset empty short-circuit, TTLSweeper failure resilience, Redactor recursion cap, OpenAI batch non-dict line, wall-clock boundary event parity, ContractStarted orphan, parse_skill_md recursion, MultiDispatcher NaN weights, evaluate_trajectory input mislabel, InstrumentedDispatcher failure telemetry, routing-lane dispatcher exclusion. |
| [0016](./0016-skill-execution-isolation.md) | Skill contract execution isolation | Accepted | `BL-133`: new `SkillContractExecutor` Protocol + `InProcessSkillContractExecutor` (default, backward-compatible) and `SubprocessSkillContractExecutor` (crash + `resource.setrlimit` isolation, opt-in). Lifts L3 from "no isolation" to "opt-in subprocess + rlimit"; container / seccomp isolation stays the out-of-tree extension point. |
| [0017](./0017-seventh-code-audit.md) | Seventh code audit, additive hardening | Accepted | `BL-215`-`218`: SkillLoader UTF-8 boundary, subprocess IPC frame length bound, `SubprocessSkillContractExecutor` metadata validation, `read_text` encoding consistency. The IPC trust boundary introduced by ADR 0016 is brought to the same "external-input-must-not-crash" invariant as the audit-path and bulk-decode sides. |
| [0018](./0018-eighth-code-audit.md) | Eighth code audit, additive hardening | Accepted | `BL-219`-`222`: JsonlSink UTF-8 write-side encoding, subprocess child-side partial-header EOF treatment, `BudgetTracker` finite/non-negative validation on caller-fed `usd` / `wall_clock_seconds`, `MultiDispatcher` per-member failure containment. Brings the BL-218 read-side standard to the write side, generalises BL-216 truncated-header handling to the child, applies the BL-159 / BL-205 NaN-clamp class to the budget input boundary, and makes the BL-207 / BL-208 telemetry-on-failure guarantee robust against ensemble-level cancellation. |
| [0019](./0019-ninth-code-audit.md) | Ninth code audit, additive hardening | Accepted | `BL-223`: `MultiSink` per-sink failure containment, the BL-222 ensemble-side guarantee generalised to the audit-sink fan-out side so a single failing sink (OTel exporter, JsonlSink with disk full) does not block downstream sinks from receiving the event; `BaseException` still propagates. The BL-202 / BL-167 audit-vs-raise parity invariant now holds at every fan-out leg. |
| [0020](./0020-tenth-code-audit.md) | Tenth code audit, additive hardening | Accepted | `BL-226` / `BL-227`: S3 user-metadata trust-boundary parsing (`_safe_float` rejects NaN / +inf / -inf / unparseable; `_safe_int` defaults the legacy-migration `0` on unparseable) applied at every metadata-read site (`S3Store._get_live`, `S3Store._sweep_sync`, `BoundedS3Store._collect_live_sync`), generalising the BL-159 / BL-201 / BL-205 / BL-215 / BL-217 / BL-221 NaN / unparseable-input invariant to the S3 metadata-read boundary; `BoundedS3Store.evict_to_capacity` per-key delete containment, generalising the BL-222 / BL-223 fan-out failure invariant and the BL-202 / BL-167 audit-vs-raise parity to the new BL-225 sequential-DELETE path. |
| [0021](./0021-eleventh-code-audit.md) | Eleventh code audit, additive hardening | Accepted | `BL-228` / `BL-229`: `RoutingChainDispatcher` per-link failure containment (the BL-222 / BL-223 / BL-227 fan-out class on the sequential cheap-first chain, the deferred ADR 0019 / 0020 revisit trigger; a raising link falls through, `BaseException` still propagates), and S3 metadata-scan HEAD not-found containment (`S3Store._head_metadata` treats a LIST-then-HEAD concurrently-deleted object as absent, mirroring `_get_live`, so `sweep_expired` / `evict_to_capacity` no longer crash on a 404; non-not-found `ClientError`s still propagate). DynamoDB `float(exp)` left unchanged with a documented rationale (the `N` type is server-validated to a finite range). |
| [0022](./0022-twelfth-code-audit.md) | Twelfth code audit, additive hardening | Accepted | `BL-231` / `BL-232`: non-finite numeric configuration validation. `BL-231` (no guard): `ActionBudget` and `RetryPolicy` reject `NaN` / `+inf` / `-inf` / negative limits at construction (the dual of BL-221, which hardened the *consumed* side of `consumed > limit` but left the *limit* side open; a `NaN` limit silently disabled the ceiling). `BL-232` (a `<= 0` guard with a `NaN` hole): `MCPServerSpec.timeout_seconds` and `TTLSweeper.interval_seconds` add a `math.isfinite` conjunct, since `NaN <= 0` / `+inf <= 0` are both False (a `NaN` sweep interval drove a no-delay busy-sweep). Generalises the BL-159 / BL-205 / BL-221 / BL-226 NaN class from the value/data boundaries to the numeric *configuration* boundaries, the peers of the `Namespace.retention_seconds` boundary BL-197 hardened. |
| [0023](./0023-thirteenth-code-audit.md) | Thirteenth code audit, additive hardening | Accepted | `BL-233`: `S3Store._sweep_sync` and `DynamoDBStore._sweep_sync` per-item network DELETE containment, extending the BL-222 / BL-223 / BL-227 / BL-228 fan-out per-member failure containment class from `evict_to_capacity` to the sibling periodic-TTL-sweep path (the open question ADR 0020 / 0021 / 0022 deferred from the BL-229 `_head_metadata` scope). A transient `SlowDown` / `ProvisionedThroughputExceeded` / network blip on one expired item no longer aborts the whole sweep pass; the inspection step (S3 HEAD, DynamoDB Scan) stays fail-loud and only the idempotent DELETE is best-effort; `BaseException` still propagates. S3's sweep and evict are now internally consistent; DynamoDB's eviction stays all-or-nothing via `_batch_write`-with-retry by design. |
| [0024](./0024-compaction-summarisation-and-tiering.md) | Memory compaction, summarisation, hot/cold tiering | Accepted | `BL-234` / `BL-235`, closing `BL-135`: the `Summarizer` Protocol + `TruncatingSummarizer` deterministic reference; `MemoryCompactor`, a driver over the ADR 0014 `VersionedMemoryStore` + `TransactionalMemoryStore` surface (version-gated atomic compaction, `atomic=False` opt-in for transaction-less backends); `TieredMemoryStore`, a hot/cold composition behind the plain `MemoryStore` surface with CAS-guarded promotion, hot-first writes with cold invalidation, cold-first deletes, version-gated demotion, and BL-212-ranked `demote_to_capacity`. No new store Protocol, no adapter changes; LRU and model-quality summarisation stay out of tree. |
| [0025](./0025-fourteenth-audit-full-pass.md) | Fourteenth audit, full-pass protocol, process hardening | Accepted | `BL-236`-`BL-239`: the stale `PYSEC-2025-183` pip-audit suppression removed (the unsuppressed audit is clean against the locked pyjwt 2.13.0), `uv lock --check` lockfile-freshness gate added to the `dependency-audit` job (closing the 2026-05-27 engagement recommendation), the unused direct `logfire` declaration dropped (the resolved graph is unchanged; logfire stays a transitive of `pydantic-ai`), and two comment-accuracy fixes (`TieredMemoryStore` stamp-map growth caveat, `_expiry` post-BL-197 wording). No runtime code finding; the ADR 0024 modules received their first audit coverage. Phase evidence under `audit/`; `BL-240` / `BL-241` proposed for maintainer decision. |
| [0026](./0026-prompt-caching-on-the-runtime-adapter.md) | Prompt caching on the runtime adapter | Accepted | `BL-132` / `BL-171`, unblocked by pydantic-ai 1.106 (verified in-session: `CachePoint`, the `AnthropicModelSettings` cache controls, `RunUsage.cache_read_tokens` / `cache_write_tokens`). `PydanticAIRuntime(model_settings=...)` forwards verbatim to the Agent (opaque to the harness, vendor-neutral like `model`, ADR 0001; `None` preserves prior behaviour); `BudgetTracker.consume_cache_tokens` + `cache_read_tokens` / `cache_write_tokens` counters surface the provider's cache hit/creation counts, deliberately not charged to `max_tokens` (upstream reports them outside `input_tokens`), with no new ceiling (pricing stays caller-fed via `consume_cost`, BL-123) and outside `snapshot()` (BL-154 carries enforced dimensions only). Live cache-hit validation coupled to `BL-120`; `LIMITATIONS.md` L9 retitled to the deterministic-only residual. |
| [0027](./0027-deferred-approval-resume.md) | Deferred (non-replay) approval resume | Accepted | `BL-114`, unblocked by pydantic-ai 1.106 (`DeferredToolRequests` / `DeferredToolResults` stable, verified in-session with an end-to-end spike). Opt-in `PydanticAIRuntime(approval_mode="deferred")`: REQUIRE_APPROVAL raises the framework's `ApprovalRequired` through a ctx-aware, schema-invisible wrapper; the leg ends with the collected approvals and the message history travels in the new optional `ResumableState.runtime_state`; resuming continues from the history (prior tool calls run exactly once) with decisions mapped onto `DeferredToolResults`, the (tool, arguments) binding re-verified at execution (BL-193 defence in depth). Deliberate divergences: denial is model-visible (`ToolDenied`), the paused leg's usage is charged, resume requires every approval decided, `stream()` stays replay-gated. Replay remains the byte-identical default; `LIMITATIONS.md` L10 rewritten. |
| [0028](./0028-hybrid-retrieval-and-decay-ranked-demotion.md) | Hybrid retrieval fusion and decay-ranked demotion | Accepted | `BL-243` / `BL-247`, the first implementation wave from the Vertex MCP analysis (#114): `memory/retrieval.py` (`fuse_rrf` Reciprocal Rank Fusion, `lexical_overlap_scores`, the `Reranker` Protocol, `HybridSemanticStore` + `HybridHit`), `InMemorySemanticStore.query_hybrid`, and `TieredMemoryStore.demote_to_capacity(rank_key=...)` + the `decay_strength` Ebbinghaus reference. Additive; vector-only `query_semantic` and FIFO demotion stay the defaults. A durable hybrid adapter and the `BitemporalMemoryStore` Protocol (`BL-250`) stay tracked. |
| [0029](./0029-graduated-authority-tiers.md) | Graduated authority tiers on the guard | Accepted | `BL-242`, the most on-thesis item from the analysis: `harness/authority.py` (`AuthorityTier` `IntEnum`, the `TierClassifier` Protocol, the `MappingTierClassifier` reference), `HarnessToolGuard(tier_classifier=, approval_tier=)` escalating a Tier-`approval_tier`-or-above action to REQUIRE_APPROVAL beyond the static `approval_required` list, `GuardResponse.tier`, and `run_under_contract` threading. Additive; no classifier preserves the flat L1 behaviour. The rollback-plan / evidence / tier-on-interruption refinements stay tracked (`BL-251`). |
| [0030](./0030-degraded-disposition-and-grounding.md) | DEGRADED disposition and grounding postconditions | Accepted | `BL-244`: `RunRecord.degraded` (record schema v1.1.0, the orthogonal quality axis beside `RunOutcome`) set by `run_under_contract` when a SOFT postcondition is violated on the final delivered leg (a clean retry clears it; substitute keeps it; escalate / hard stay their own terminals), and `harness/grounding.py` (the pure `ungrounded_citations` anti-confabulation check + the SOFT `grounding_predicate` factory) that relabels without rewriting model content. Additive; the field defaults False and the schema bump is forward-compatible. |
| [0031](./0031-approval-context-payload.md) | Approval-context payload on the interruption | Accepted | `BL-251` (data-carrying half): `ApprovalInterruption.tier` and `ApprovalInterruption.rollback_plan` now travel onto the human-facing pause, symmetrically across the replay and deferred resume paths (the deferred gate records `(tier, rollback_plan)` by `tool_call_id`). New `RollbackPlanner` Protocol + `MappingRollbackPlanner` reference (the `TierClassifier` injection stance), `GuardResponse.rollback_plan`, `HarnessToolGuard(rollback_planner=)` consulted only on the approval branch, and `run_under_contract(rollback_planner=)` threading (a planner alone does not build a guard). Additive; both fields default None, no schema change. The behavioural half (evidence hook, two-step parameter restatement) is split forward to `BL-252`. |
| [0032](./0032-bitemporal-fact-store.md) | Bitemporal fact store | Accepted | `BL-250` (the held-out half of `BL-247`): `memory/bitemporal.py` with the `BitemporalMemoryStore` Protocol + `InMemoryBitemporalStore` reference. Facts addressed by `(subject, predicate)` with validity time (`valid_from`/`valid_to`) separate from transaction time (`recorded_at`/`superseded_at`/`superseded_by`), a per-fact confidence, and auto-supersession; `record` / `current` / `as_of` / `history`. A standalone Protocol, not a `MemoryStore` extension (a different addressing model, the ADR 0024 stance). Additive; no schema change. Durable adapters and a fact-keyed audit are follow-ups. |
| [0033](./0033-two-step-restatement.md) | Two-step parameter restatement for irreversible actions | Accepted | `BL-252` (resume-verification half): an irreversible (Tier 3) approval is honoured only when the human re-enters the arguments and they match (`ApprovalInterruption.restated_arguments`, `ResumableState.approve(restated_arguments=)`, `_restate_satisfied` in both the replay and deferred gates), composing with the BL-193 binding; a missing or mismatched restatement re-pauses. Additive (Tier 3 only; nothing in tree currently resumes-and-executes a Tier 3 approval). The evidence-capture hook is split forward to `BL-253`. |
| [0034](./0034-operational-memory-journal.md) | Structured operational-memory journal | Accepted | `BL-245` (the largest analysis item): `memory/journal.py`, typed records (`Task` / `Thread` / `Decision` / `Event`) plus a `Journal` driver over a `MemoryStore` (records inherit namespace / TTL / audit / encryption). A task FSM with an explicit transition table (`_TASK_TRANSITIONS`; illegal move raises `InvalidTransition`, every transition appends to the log) + a `ready_tasks` dependency query; a thread stale-after window via `stale_threads` / `touch_thread`; the decision log and a categorized `timeline`. A driver, not a store Protocol (the `MemoryCompactor` precedent). Additive; no schema change. Version-gated mutation, cycle rejection, and the `BL-249` `context_pack` are follow-ups. |
| [0035](./0035-fallback-chain.md) | Graceful degradation ladder around the Runtime | Accepted | `BL-248`: `harness/fallback.py: FallbackChain`, a `Runtime` wrapping an ordered list of runtimes and trying each until one returns, the degradation ladder `RetryPolicy` is not. Composes with `RetryPolicy`; `default_should_descend` descends on a non-`HarnessError` `Exception` (a governance / budget / approval halt never reroutes), an approval pause is returned not fallen back, `BaseException` propagates, `stream` delegates to the first member. Additive (a `Runtime` composition); no schema change. |
| [0036](./0036-read-side-freshness.md) | Read-side freshness gating and refusal-as-data | Accepted | `BL-246`: `harness/freshness.py` with `require_fresh(extract, max_age_seconds)` (a SOFT-default `Predicate` factory gating a value by age against an injected clock, the `grounding_predicate` shape; HARD makes it terminal), the pure `is_stale` helper, and `Refusal`, a typed model-legible refusal. Additive; a workload wraps `Refusal` in its output model. The full `{ok, value, refusal}` envelope stays a workload concern. |
| [0037](./0037-session-rehydration.md) | Session rehydration over the journal | Accepted | `BL-249` (unblocked by ADR 0034): `memory.journal.context_pack` + `ContextPack`, an async assembler packing ready / in-progress tasks, stale vs open threads, and recent decisions from a `Journal` for a session-start context refresh. The scheduled single-shot envelope is a documented deployment pattern, not a contract change; a packaged workload bundle and a delta mode are follow-ups. Additive; no schema change. |
| [0038](./0038-evidence-capture-hook.md) | Evidence-capture hook around an irreversible action's execution | Accepted | `BL-253` (the held-out half of `BL-252`, closing the Vertex MCP analysis wave `BL-242`-`BL-253`): `harness/evidence.py` (the `EvidenceContext` call-context, the `EvidenceHook` Protocol `before(context) -> token` / `after(token, *, error=None)`, the `RecordingEvidenceHook` reference), a gate-to-wrapper tier signal (`_gate` / `_deferred_gate` return a frozen `_GateResult(soft, tier, rollback_plan)`), the shared `_with_evidence` bracket on all three tool-execution paths (replay-local, deferred-local, MCP), and the opt-in `PydanticAIRuntime(evidence_hook=)`. Fires only for an `IRREVERSIBLE` (Tier 3) action with a hook configured (`before`, body, `after` in a `finally` with the body's exception, so a failed action is still recorded); the opaque token pairs before/after for concurrency. Additive; no L1 / Runtime-Protocol / schema change. |
| [0039](./0039-fifteenth-code-audit.md) | Fifteenth code audit, additive hardening | Accepted | `BL-254`-`BL-263`, the first audit pass over the Vertex MCP analysis wave (ADR 0028-0038). `BL-254`: `LocalSkillSource.fetch` refuses a hardlink (`st_nlink > 1`), extending the symlink refusal so a crafted mirror cannot exfiltrate a host secret via a second directory entry. `BL-256` / `BL-258`: `DriftMonitor.record` and `InMemorySemanticStore.query_hybrid` reject a non-finite increment / reranker score, extending the BL-159 / BL-205 / BL-221 / BL-231 NaN class to two run-time value boundaries. `BL-257`: `fuse_rrf` ranks by distinct position so a duplicate id no longer penalises the RRF score of later unique ids. `BL-259`: the SQLite TTL-expiry clock is read after `BEGIN IMMEDIATE` in `mset` / `compare_and_set` / `write_versioned` / `transact`, so the snapshot the write commits against is the one the TTL is computed from (the read-vs-CAS expiry boundary class on the write path). `BL-260`: `S3Store`'s list-then-HEAD liveness check (`_head_live`) no longer lazy-deletes from the read path, generalising the BL-188 read-vs-listing expiry boundary. `BL-262`: `BoundedRedisStore.write` performs the value `SET` and the capacity-index `ZADD` under one `MULTI`/`EXEC` pipeline, so a crash cannot leave the index and the value store inconsistent. `BL-255` / `BL-261`: the `grounding._grounded` `any`-short-circuit and the evidence-hook fail-safe before/after ordering docstring. Two non-findings recorded. Additive; no L1 / Protocol / schema change. |

See [docs/backlog.md](../backlog.md) for the line-item tracker and
[CLAUDE.md](../../CLAUDE.md) for when a change needs a new ADR.
